# migpt

python==3.10.11

#Steps to follow:

1. Make sure the python version installed is 3.10.11
2. Clone the repo.
3. Move to the repo directory.
4. Run python3 -m venv venv to create a virtual environment.
5. Run . venv/bin/activate
6. pip install -r requirements.txt
7. Run source env-sample.sh to export all the Environment Variables
